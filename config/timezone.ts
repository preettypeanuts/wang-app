/** App-wide calendar timezone (WIB). */
export const APP_TIMEZONE = "Asia/Jakarta";

/** Jakarta has no DST — stable offset for date-only ISO strings. */
export const APP_TIMEZONE_OFFSET = "+07:00";
