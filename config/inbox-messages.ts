/** Initial + per-page inbox fetch — ~10 user/assistant pairs (20 bubbles). */
export const INBOX_MESSAGE_PAGE_SIZE = 20;

/** Distance from top (px) before requesting older messages. */
export const INBOX_LOAD_OLDER_SCROLL_THRESHOLD = 120;
