/** Outer shells — sidebar panel, large floating surfaces. */
export const SEPARATED_SHELL = "rounded-[1.75rem]";

/** Tiles, bubbles, cards in separated UI. */
export const SEPARATED_SURFACE = "rounded-2xl";

/** Buttons & controls inside separated UI. */
export const SEPARATED_CONTROL = "rounded-xl";

/** Circular pill controls. */
export const SEPARATED_PILL = "rounded-full";
